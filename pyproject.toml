[project]
name = "raktr"
version = "0.1.0"
description = "The next generation modern c++ 3D game engine — Where Worlds Take Shape."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cmake>=4.2.1",
    "conan>=2.25.2",
]
